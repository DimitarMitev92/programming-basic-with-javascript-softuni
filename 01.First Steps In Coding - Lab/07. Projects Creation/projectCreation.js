function projectsCreations(input){
    const name = input[0];
    const projectsNum = Number(input[1]);
    console.log(`The architect ${name} will need ${projectsNum*3} hours to complete ${projectsNum} project/s.`)
}
